{>"layouts/master" /}

{<body}
        <div id="instructions">
            <h1>Draw anywhere!</h1>
            <h2>You will see everyone else who's doing the same.</h2>
            <h3>Tip: if the stage gets dirty, simply reload the page</h3>
        </div>
        <div id="cursors"></div>
        <canvas id="paper" width="1500" height="800">
            Your browser needs to support canvas for this to work!
        </canvas>
{/body}
